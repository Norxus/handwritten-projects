# handwritten_mem0

用于抄写与学习 `mem0` 记忆系统核心实现的空项目。

## 目录约定

- `mem0/`: 逐步抄写的核心包代码
- `.venv/`: 本地虚拟环境

## 快速开始

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e ".[dev,nlp]"
```
